# 小黄人 (Minion) Roadmap / 未完成清单 — 2026-07-04

> 当前主状态文档,取代 [2026-06-17](2026-06-17.md)（及在飞 PR #184 中的 2026-06-19）。
> 本版特点:未完成项**逐条对代码核实过**（带证据行号),纠正了此前若干"缺口"误报;
> 历史工程债明细见 [2026-05-22](2026-05-22.md) / [architecture-todos/2026-04-08.md](../architecture-todos/2026-04-08.md)。
>
> 图例:✅ 完成 · 🟡 部分/卡外部 · ⬜ 未开始 · 🔴 风险卡点

## 0. 一句话
runner 大脑侧(Java)**基本完工、有测试**;"没搞的"大头是 **M2/M3 = 上 Pi + 硬件 + 固件 + Realizer**（卡硬件/跨仓,不在 runner）。卡点性质是**硬件 / 你的 key / 真外部实例 / 跨仓 Python**,不是 runner 缺代码。

## 1. 里程碑总表
| 里程碑 | 状态 | 卡什么 |
|---|---|---|
| M0 全仿真闭环 | ✅ | — |
| M1 真云语音 <1.3s（无硬件） | 🟡 近 | ① 跑 bench 量数（DashScope key） ② voice-gateway 接 inbound.interrupt + 共享 schema ③ TTS viseme 实测 |
| M2 上 Pi 能动能看 | ⬜ 大头 | 硬件到位（Pi / 舵机 / 眼 / 麦 / 摄像头）+ minion-body B4/B6 |
| M3 拟人化 / 表情同步 | ⬜ 最远 | 依赖 M2 + minion-body B5 Realizer + 主动触发 + 人格 Skills |

## 2. 详细未完成项

### 2.1 M1 真云语音（runner 侧就绪,差量数 + 跨仓）
- 🔴 **跑 bench 填数**:`LatencySpikeBench` 从没真跑出有意义数（OpenRouter 被 provider 排队主导）;需 DashScope key 填 `docs/latency-spike-results.md`（在 PR #184）。
- 判 eager / max-wait 是否接生产（依赖 bench 数）。
- 跨仓 voice-gateway:接 `inbound.interrupt` 帧 + 采用共享 schema `contract/minion-voice-text/messages.schema.json`。
- TTS 厂商 viseme / streaming 实测（Azure / Polly / Aliyun）。

### 2.2 M2 上 Pi（卡硬件,非 runner 代码）
- minion-body **B4**:真 GPIO MCP server + streamable_http（ADR-008）,完成度待核。
- minion-body **B6**:舵机 / ESP32-S3 眼 / 麦 / 摄像头硬件。
- runner 侧就绪:`RealPiConnectionIT`（门控 `MINION_PI_URL`）等真机;ADR-011 重连仅对假 Pi 验过。

### 2.3 M3 拟人化（依赖 M2）
- minion-body **B5** 全量 Realizer:viseme 口型 + emotion 眼 + bookmark 手势 + idle,对齐音频时钟。
- 主动 `TriggerSource`（看到人脸打招呼）、人格 Skills。
- minion-eyes:`LOOK`（注视方向,待视觉任务定义）;真 AMOLED 板 smoke。

### 2.4 跨仓三仓（runner 契约就绪,等对接）
- **voice-gateway**（Python）:inbound.interrupt / 共享 schema / 全链 TTFA harness / 预生成。
- **minion-body**（Python/Pi）:B4 / B5 / B6。
- **minion-eyes**（ESP32-S3）:LOOK / 真硬件 smoke。

### 2.5 已合入 main（经多代理对抗审查 + 按审查修复,2026-07-04）
- **#184 latency phase-1** ✅ 已合:代码/测试到位;**剩** bench 真数待填（需 DashScope key）、eager→生产决策待定。
- **#183 OpenClaw 换脑** ✅ 已合（假对端验证）:**剩真用待补** 真网关实例、`connect` auth 握手（challenge + 设备签名）、`session.tool` payload、持久连接 + 重连、persona `SOUL.md`、Pi MCP 挂 OpenClaw。

### 2.6 工程债（逐条核实;不阻塞里程碑）
**✅ 其实已做（勿再当 TODO）**:context compaction（`SnipCompactor`/`MicroCompactor`,`AgentLoop.java:112/237` 真调 `compact()`）、子代理委派（`SpawnSubagentTool`）、pre/post-tool hooks（`ToolCallingLoop.java:318/329`）、`LLMProviderFactory` ServiceLoader SPI、`ToolSourceProvider` 生命周期。`PromptSection` 模块化 prompt = **按设计废弃**（被 Skill 取代,见 `SkillUnificationAcceptanceTest`）,非 TODO。

**CostTracker 预算护栏是死的（技术上真缺口,但优先级不高 —— 2026-07-04 用户判定,暂缓）**:`ToolCallingLoop.java:249` 查了 `costTracker.isOverBudget()`,但全仓**无任何 `costTracker.record(...)` 调用** → tracker 永空 → 预算永不触发。修点已定位:`ToolCallingLoop.java:300` 已有 `LLMResponse response`,`getUsage().getInputTokens()/getOutputTokens()`（`LLMResponse.java:81-94`）+ `CostTracker.record(int,int)`（`CostTracker.java:31`）都在。配套:核 provider 是否填 `usage`;`TokenCounter`（现返 null 且无消费方）作兜底。→ 记录在案,不急做。

**🟡 Tier2（中价值）**:
- SessionManager 生命周期没接:`onSessionStart/onSessionStop` 是 `default {}` no-op、**无生产调用方**;WS `afterConnectionEstablished:140` / `afterConnectionClosed:181` 是天然触发点。
- 真 web 搜索:`WebTools.java:38` `// TODO: Integrate with Brave / Serper`,现为 mock。
- 外部 HookHandler（shell / webhook）缺:仅进程内 `AgentObserver`。

**⚪ Tier3（低优先 / 现状可接受）**:内存全同步阻塞（`MemoryProvider.getHistory` 裸 List、无 reactive）、无 feature-flag 框架（`@ConditionalOnProperty` 已覆盖部署级）、无 OTel 后端桥（Tracer 只导出 `StreamEvent.TRACE`）、无 `LLMProviderChainBuilder`（手工组合能用）、多数工具"假流式"（只 `CliTool` 真流）、agent-sdk `DefaultAgent.chatStream()` 是桩（非生产路径）、MessageSnapshot 部分采用。
> 更多历史工程债（三执行路径一致性、工具超时等）见 [2026-05-22](2026-05-22.md)。

## 3. 卡点性质归类
- **卡硬件**:M2 / M3 全部（Pi / 舵机 / 眼 / 麦 / 摄像头 + minion-body / eyes 固件）。
- **卡你的 key**:M1 bench 量数（DashScope）、真 web 搜索（搜索 API key）。
- **卡真外部实例**:#183 OpenClaw 真网关。
- **卡跨仓 Python**:voice-gateway / minion-body / minion-eyes。
- **纯 runner、现在就能干**:工程债 Tier1/2（CostTracker 接线、SessionManager 生命周期…）。

## 4. 建议顺序
1. ~~先合两条已验证 PR（#183 / #184）收口~~ ✅ 已合入 main（含 review 修复）。
2. M1 收尾:你给 key → 跑 bench;跨仓接 voice-gateway `inbound.interrupt`。
3. 无外部依赖的工程债 Tier2（SessionManager 生命周期 / 真 web 搜索 / 外部 HookHandler）—— 按需挑。（CostTracker 暂缓,见 §2.6。）
4. M2 / M3 等硬件到位再上（runner 侧已就绪）。
