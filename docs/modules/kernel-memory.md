# kernel-memory

File-based + SQLite persistent memory with hybrid BM25 + vector search.

## Responsibility
- FileMemoryManager: ephemeral/durable memory
- MemoryIndex: BM25 keyword + vector semantic hybrid search
- Chunking, embedding, queuing

## Dependencies
agent-kernel only.
